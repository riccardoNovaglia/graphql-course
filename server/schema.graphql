type Query {
    jobs: [Job]
    companies: [Company]
    company: Company
}

type Company {
    id: ID!
    name: String
    description: String
}

type Job {
    id: ID!
    title: String
    description: String
    company: Company
}
